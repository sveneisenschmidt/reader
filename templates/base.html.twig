<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Reader{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
</head>
<body{% block body_class %}{% endblock %}>
    <input type="checkbox" id="menu-toggle" class="menu-checkbox">
    <header>
        <label for="menu-toggle" class="menu-toggle">Menu</label>
        <h1><a href="{{ path_with_filters('feed_index') }}" class="home">Reader</a></h1>
        <nav>
            <a href="{{ path_with_filters('subscriptions') }}">Subscriptions</a>
        </nav>
    </header>

    <main>
        {% block main %}{% endblock %}
    </main>

    <footer>
        <span>Reader is open source and <a href="https://github.com/sveneisenschmidt">available on GitHub</a></span>
        {% set refreshTime = last_refresh() %}
        {% if refreshTime %}
        <span class="last-refresh">Refreshed {{ refreshTime|ago }}</span>
        {% endif %}
    </footer>

    {% block scripts %}
    <script src="{{ asset('js/pull-to-refresh.js') }}"></script>
    {% endblock %}
</body>
</html>
